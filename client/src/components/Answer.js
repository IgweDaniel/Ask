import React from "react";
import styled from "styled-components";

import { VerticalAlign } from "../genericStyle";

const Answer = ({ className }) => {
  return <div className={className}>Answer</div>;
};

export const StyledContentHeader = styled(Answer)``;
